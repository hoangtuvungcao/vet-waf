#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
export DH_VERBOSE=1

DEB_NAME=vet-waf

%:
	dh $@ --with systemd

override_dh_auto_install:
	dh_install fw usr/src/vet_waf
	dh_install db usr/src/vet_waf
	dh_install tls usr/src/vet_waf
	dh_install lib usr/src/vet_waf
	dh_install regex usr/src/vet_waf
	dh_install Makefile usr/src/vet_waf
	dh_install etc/* /etc/vet_waf
	dh_install scripts /lib/vet_waf
	find "debian/$(DEB_NAME)/usr/src/vet_waf" -name '.git*' -exec rm {} \;

override_dh_installinit:
	dh_installinit --no-start --no-restart-on-upgrade --noscripts --name=vet-waf

override_dh_systemd_start:
	dh_systemd_start --no-start --no-restart-on-upgrade

override_dh_auto_build:
override_dh_auto_install:
override_dh_auto_clean:
override_dh_auto_test:
